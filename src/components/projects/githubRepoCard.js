import { Card, CardHeader, Typography } from '@mui/material';
import React from 'react';
import './styles/githubRepoCard.scss';

const GithubRepoCard = () => {
  return (
    <div>
      <Typography>this is acard</Typography>
    </div>
  );
};

export default GithubRepoCard;
